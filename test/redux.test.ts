import { expect, test } from 'vitest';

// import {
//   addSelectedFood,
//   updateSelectedFoodNumber,
//   removeSelectedFood,
//   setSelectedFoods,
//   resetSelectedFoods,
// } from '../src/stores/selectedFoodsSlice';

const sum = (a, b) => a + b;

test('adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
});

// TODO: write unit test to test selectedFoodsSlice
